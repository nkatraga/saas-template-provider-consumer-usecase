// [Template] — Next.js configuration. Add redirects, rewrites, and plugin config here.
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: false,
};

export default nextConfig;
